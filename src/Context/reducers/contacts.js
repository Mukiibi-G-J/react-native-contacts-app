export const contacts = (state, action) => {
  switch (action.type) {
    case 'GET_CONTACTS':
      return state;

    case 'FILTER_CONTACTS':
      return state;
    default:
      return state;
  }
};
